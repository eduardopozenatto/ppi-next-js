---
phase: 6
plan: 1
title: "Autenticação e Autorização (JWT)"
wave: 1
depends_on: ["05-02"]
files_modified:
  - backend/package.json
  - backend/src/config/env.ts
  - backend/src/app.ts
  - backend/prisma/seed.ts
  - backend/src/schemas/auth.schema.ts
  - backend/src/utils/crypto.ts
  - backend/src/utils/jwt.ts
  - backend/src/middlewares/auth.ts
  - backend/src/controllers/auth.controller.ts
  - backend/src/routes/auth.routes.ts
  - backend/src/routes/index.ts
autonomous: true
requirements_addressed: [BAUTH-01, BAUTH-02, BAUTH-03, BAUTH-04, BAUTH-05, BAUTH-06, BPERM-03]
---

<objective>
Implementar a fundação de segurança do backend: autenticação baseada em JWT (HttpOnly Cookies), hash de senhas (bcrypt), e autorização com Request Context (`req.user`), populando as 11 permissões definidas pelo frontend. Ajustar o seed para que as contas de mock funcionem corretamente com hashes.
</objective>

<must_haves>
- Instalação das dependências `bcryptjs`, `jsonwebtoken`, e `cookie-parser`.
- Zod Schemas para os dados de `/register` e `/login`.
- Geração e verificação segura de Hashes e JWTs isoladas em utilities.
- Middleware `requireAuth` lê HttpOnly tokens, valida com a signature, e carrega dados completos de `req.user`.
- Criação dos endpoints: `POST /api/auth/register`, `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`.
- A rota `/me` retorna exatamente a mesma estrutura vista no `LabSessionUser` na Phase 5.
- Atualização do arquivo original `prisma/seed.ts` para que todas contas nasçam com hashes validadas para `"1234"` para propósitos de dev/demo.
</must_haves>

<tasks>

<task id="06-01-01" title="Adicionar dependências e atualizar environment">
<read_first>
- backend/package.json
- backend/src/config/env.ts
</read_first>
<action>
1. Execute o install das dependencias via NPM/PNPM:
`pnpm add bcryptjs jsonwebtoken cookie-parser` e em devs `@types/bcryptjs @types/jsonwebtoken @types/cookie-parser`.
2. Adicione `JWT_SECRET` e `JWT_EXPIRES_IN` (padrão '7d') no backend/src/config/env.ts utilizando tipagem normal do `z`.
3. Adicione `JWT_SECRET` local para desenvolvimento no `.env`.
</action>
<acceptance_criteria>
- Packages e types instalados.
- Validadores env do backend não quebram ao reiniciar sem o `JWT_SECRET` declarado no arquivo `.env`.
</acceptance_criteria>
</task>

<task id="06-01-02" title="Criar Schemas Zod e Utilitários de Segurança (crypto / jwt)">
<read_first>
- backend/src/config/env.ts
</read_first>
<action>
Criar arquivo de `backend/src/schemas/auth.schema.ts` com validações para `name`, `email`, `password`, `matricula`.
Criar os helpers `crypto.ts` com Bcrypt e `jwt.ts` com geração de token referenciando o `env.JWT_SECRET`.
</action>
<acceptance_criteria>
- As funções `hashPassword` e `comparePassword` funcionam corretamente.
- `signToken` aceita e embute o ID de usuário (`userId`).
</acceptance_criteria>
</task>

<task id="06-01-03" title="Construir Middlewares Auth/Permissões">
<read_first>
- backend/src/utils/response.ts
- frontend/types/lab-session.ts
</read_first>
<action>
Codificar `backend/src/middlewares/auth.ts`:
- O `requireAuth` analisa chamadas via Cookies.
- Implemente uma tipagem global Typescript Express `declare global { namespace Express { interface Request {...} } }` englobando `user`.
- O `requirePermission(permission: keyof TagPermissions)` checará se a chave de permissão é verdadeira no Payload injetado no Auth.
</action>
<acceptance_criteria>
- A tipagem Request extendida permite mapear livremente objetos da requisição.
- Chamadas não autenticadas retornam `401 Unauthorized`.
- O payload montado tem todas as chaves extraídas do Model `Tag` fundido adequadamente com `UserPermissionOverride`.
</acceptance_criteria>
</task>

<task id="06-01-04" title="Codificar AuthController e Integrar ao Express router">
<read_first>
- backend/prisma/schema.prisma
</read_first>
<action>
A parte final do controller:
1. `auth.controller.ts` para resolver os registros lendo o Zod.
2. Anexar à `auth.routes.ts`.
3. Descomentar `authRouter` no índice principal `index.ts`.
4. Incluir `app.use(cookieParser())` imediatamente antes das sub-rotas Express no `app.ts`.
</action>
<acceptance_criteria>
- `register` insere usuários no banco (cuid default id).
- `login` seta cookies.
- Rotas validadas.
</acceptance_criteria>
</task>

<task id="06-01-05" title="Atualizar a injeção do Seed">
<read_first>
- backend/prisma/seed.ts
</read_first>
<action>
- Utilize a nova função de `hashPassword` de `src/utils/crypto.ts` em `seed.ts` (ou passe bcrypt direto) para salvar um hasheado da string `"1234"` para ser a senha de todos.
- Rerotar as passwords para `$2a$...` na declaração dos `user.upsert`.
</action>
<acceptance_criteria>
- Seed roda sem problemas.
- No BD final (studio) a senha foi armazenada como string protegida Bcrypt.
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Verify Auth Login behavior

# Postman / curl (login com usuario mocado "carlos.aluno.lab@gmail.com")
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"carlos.aluno.lab@gmail.com", "password":"1234"}' -i
# -> Deve retornar status 200, JWT cookie e dados básicos.
```
</verification>
