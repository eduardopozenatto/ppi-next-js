---
phase: 7
plan: 1
title: "CRUD de Inventário e Empréstimos"
wave: 1
depends_on: ["06-01"]
files_modified:
  - backend/package.json
  - backend/src/app.ts
  - backend/src/schemas/inventory.schema.ts
  - backend/src/schemas/loan.schema.ts
  - backend/src/controllers/inventory.controller.ts
  - backend/src/controllers/loans.controller.ts
  - backend/src/routes/inventory.routes.ts
  - backend/src/routes/loans.routes.ts
  - backend/src/routes/index.ts
autonomous: true
requirements_addressed: [BINV-01, BINV-02, BINV-03, BINV-04, BINV-05, BINV-06, BLOAN-01, BLOAN-02, BLOAN-03, BLOAN-04, BLOAN-05]
---

<objective>
Construir o Core da API (Inventário e Empréstimos) e conectar os Controladores aos modelos de Banco de Dados de forma segura. Usar transações atômicas para deduções lógicas do estoque para que não hajam race conditions, e habilitar upload simples de imagens estáticas via interface `Multer`.
</objective>

<must_haves>
- API de inventário listando os Itens com Paginação (`PaginatedResponse`) com suporte a buscas por nome ou categoria.
- Integração de `multer` em `/uploads` para arquivos de inventário.
- Controllers protegidos pelo guardião de permissões da Phase 6 (`requireAuth`, `requirePermission(xyz)`).
- API de Empréstimos (`/api/loans`) lidando com `Prisma.$transaction` ao tentar agendar um loan e abater/devolver itens da `availableQuantity`.
</must_haves>

<tasks>

<task id="07-01-01" title="Multer Setup e Pastas Public">
<read_first>
- backend/package.json
- backend/src/app.ts
</read_first>
<action>
1. Execute o npm instl: `pnpm add multer` e `pnpm add -D @types/multer`.
2. Em `app.ts`, insira `app.use('/uploads', express.static('public/uploads'))`.
3. Garanta que a diretório public/uploads exista (coloque em gitignore dependendo)
</action>
<acceptance_criteria>
- Ao visitar GET /uploads/arquivo.png a porta servirá o FS.
</acceptance_criteria>
</task>

<task id="07-01-02" title="Criar Schemas Zod">
<read_first>
- backend/src/schemas/auth.schema.ts
</read_first>
<action>
- Em `inventory.schema.ts`: schema para criação/edição e validação do GET de queryParams.
- Em `loan.schema.ts`: schema de submit (`[{inventoryItemId, quantity}]` + datetime `dueDate`).
</action>
<acceptance_criteria>
- Input de arrays no Loan submit mapeados perfeitamente à tipagem `LoanItem`.
</acceptance_criteria>
</task>

<task id="07-01-03" title="Codificar Inventory Controller & Router">
<read_first>
- backend/src/utils/response.ts
</read_first>
<action>
- `backend/src/controllers/inventory.controller.ts` > Exportar GETs, POSTs, PUT e DELETE.
- Integrar os métodos `multer` local (disco storage engine) para a rota extra de `/api/inventory/:id/image`.
- Fixar o `inventoryRouter` na `backend/src/routes/inventory.routes.ts`.
</action>
<acceptance_criteria>
- Rota GET / suporta parametros de string Search e Offset de Pagination.
- Somente adm pode usar POST.
</acceptance_criteria>
</task>

<task id="07-01-04" title="Codificar Loan Controller & Router c/ Transações">
<read_first>
- backend/src/controllers/inventory.controller.ts
</read_first>
<action>
- `backend/src/controllers/loans.controller.ts`: 
  - `POST /` faz Prisma iterar sobre a query Items e conferir disponibilidades, e injeta com Transaction o decrease.
  - `PUT /:id/status` altera `pending` para `active` aumentando `loanedQuantity`. 
  - Admin view retorna com Includes, user view retorna somente `.where({ borrowerId: req.user.id })`.
</action>
<acceptance_criteria>
- Race Conditions blindadas pelo transacionador Prisma do PostgreSQL.
- Permissoes como `pedir_emprestimos` injetadas no Routes.
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Validando lista
curl -s http://localhost:3001/api/inventory | jq '.data.items'

# Submissao de test loan com Token
# ...
```
</verification>
