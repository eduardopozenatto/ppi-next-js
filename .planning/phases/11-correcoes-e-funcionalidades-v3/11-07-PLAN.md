---
wave: 1
depends_on: []
files_modified:
  - "backend/src/controllers/auth.controller.ts"
  - "frontend/app/recovery/RecoveryForm.tsx"
autonomous: true
---

# Gap 2: Recuperação de Senha (Email não recebido)

## Objective
Fornecer o código de recuperação no ambiente de desenvolvimento para que o fluxo possa ser testado sem um servidor SMTP real configurado.

## Tasks

<task>
  <read_first>
    - backend/src/controllers/auth.controller.ts
    - frontend/app/recovery/RecoveryForm.tsx
  </read_first>
  <action>
    1. No `backend/src/controllers/auth.controller.ts`, na função `requestPasswordReset`, adicione o código gerado à resposta JSON se o ambiente não for produção: `res.json({ message: "...", devCode: process.env.NODE_ENV !== "production" ? code : undefined });`.
    2. No `frontend/app/recovery/RecoveryForm.tsx`, no método `handleRequestCode`, após o sucesso da API, verifique se `res.devCode` existe. Se existir, adicione um toast: `addToast({ title: "Ambiente de Teste", message: "Seu código é: " + res.devCode, variant: "info" });`.
  </action>
  <acceptance_criteria>
    - `backend/src/controllers/auth.controller.ts` contains `devCode:`
    - `frontend/app/recovery/RecoveryForm.tsx` contains `devCode`
  </acceptance_criteria>
</task>
