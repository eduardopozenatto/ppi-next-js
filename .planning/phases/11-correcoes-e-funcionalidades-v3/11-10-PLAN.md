---
wave: 1
depends_on: []
files_modified:
  - "frontend/app/(auth)/items/page.tsx"
  - "frontend/components/inventory/InventoryCatalogGrid.tsx"
  - "frontend/components/inventory/InventoryManagementTable.tsx"
autonomous: true
---

# Gap 5: Problemas no Catálogo e Gerenciamento de Estoque

## Objective
Corrigir a renderização de imagens no catálogo, alinhar o modal de visualização, corrigir o hard-delete de inventário para sumir da tela, e consertar a pesquisa/filtro na aba de buscar itens.

## Tasks

<task>
  <read_first>
    - frontend/app/(auth)/items/page.tsx
    - frontend/components/inventory/InventoryCatalogGrid.tsx
    - frontend/components/inventory/InventoryManagementTable.tsx
  </read_first>
  <action>
    1. No `frontend/app/(auth)/items/page.tsx`, implemente corretamente a lógica de busca por `searchQuery` e filtro por `category` no frontend (filtrando o array `items` retornado pela API) para que a pesquisa funcione em tempo real. Exemplo: `items.filter(item => item.name.toLowerCase().includes(search.toLowerCase()) && (category === "all" || item.categoryId === category))`.
    2. No `frontend/components/inventory/InventoryCatalogGrid.tsx`, garanta que `getStaticUrl` esteja sendo chamado para a propriedade de imagem (como feito na tabela) para que ela apareça na listagem do catálogo.
    3. No `frontend/components/inventory/InventoryManagementTable.tsx`, na lógica de `handleDelete`, após sucesso na API, atualize o estado para remover o item da tela: `setItems(prev => prev.filter(i => i.id !== id))`. Se houver algum ViewModal em `items`, corrija o alinhamento das classes Tailwind.
  </action>
  <acceptance_criteria>
    - `frontend/app/(auth)/items/page.tsx` implements `.filter(` for search term
    - `frontend/components/inventory/InventoryManagementTable.tsx` removes the item from state on successful delete
  </acceptance_criteria>
</task>
