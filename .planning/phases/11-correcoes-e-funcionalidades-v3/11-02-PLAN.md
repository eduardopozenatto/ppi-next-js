# Plan 11-02: Alteração de Senha via Email com Código de Verificação

---
wave: 1
depends_on: []
files_modified:
  - backend/src/controllers/auth.controller.ts
  - backend/src/routes/auth.routes.ts
  - backend/src/schemas/auth.schema.ts
  - backend/src/utils/email.ts
  - backend/prisma/schema.prisma
  - backend/package.json
  - frontend/app/(auth)/settings/profile/ProfileSummary.tsx
  - frontend/app/(public)/recovery/page.tsx
autonomous: true
requirements: [FAUTH-03]
---

## Objetivo

Implementar um fluxo real e funcional de alteração de senha em duas situações:
1. **Usuário logado** — altera a senha no perfil (informando senha atual + nova senha)
2. **Recuperação de senha** — usuário não logado solicita reset via email, recebe código de 6 dígitos, valida, e define nova senha

## must_haves

- Endpoint `POST /api/auth/change-password` para usuários logados (valida senha atual)
- Endpoint `POST /api/auth/forgot-password` que envia email com código de 6 dígitos
- Endpoint `POST /api/auth/verify-reset-code` que valida o código
- Endpoint `POST /api/auth/reset-password` que define nova senha usando o código verificado
- Código expira em 15 minutos
- Nodemailer configurado para envio de email (SMTP configurável via .env)
- Frontend do perfil chama `/change-password` com os campos existentes
- Frontend da página de recuperação tem fluxo de 3 steps: email → código → nova senha

## Tarefas

<task id="11-02-T1">
<title>Instalar nodemailer e criar utilitário de email</title>

<read_first>
- backend/package.json (dependências existentes)
- backend/.env (variáveis de ambiente)
</read_first>

<action>
1. Instalar dependência: `pnpm add nodemailer` e `pnpm add -D @types/nodemailer` no diretório backend

2. Criar `backend/src/utils/email.ts`:
```ts
import nodemailer from 'nodemailer';

const transporter = nodemailer.createTransport({
  host: process.env.SMTP_HOST || 'smtp.gmail.com',
  port: Number(process.env.SMTP_PORT) || 587,
  secure: false,
  auth: {
    user: process.env.SMTP_USER,
    pass: process.env.SMTP_PASS,
  },
});

export async function sendResetCodeEmail(to: string, code: string): Promise<void> {
  await transporter.sendMail({
    from: process.env.SMTP_FROM || '"LabControl" <noreply@labcontrol.app>',
    to,
    subject: 'LabControl — Código de recuperação de senha',
    html: `
      <div style="font-family: sans-serif; max-width: 480px; margin: 0 auto; padding: 32px;">
        <h2 style="color: #1a1a2e;">Recuperação de Senha</h2>
        <p>Você solicitou a alteração de senha no LabControl.</p>
        <p>Seu código de verificação é:</p>
        <div style="background: #f0f0f5; border-radius: 12px; padding: 20px; text-align: center; margin: 24px 0;">
          <span style="font-size: 32px; font-weight: bold; letter-spacing: 8px; color: #6366f1;">${code}</span>
        </div>
        <p style="color: #666; font-size: 14px;">Este código expira em <strong>15 minutos</strong>.</p>
        <p style="color: #666; font-size: 14px;">Se você não solicitou esta alteração, ignore este email.</p>
      </div>
    `,
  });
}
```

3. Adicionar ao `.env`:
```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="LabControl" <noreply@labcontrol.app>
```
</action>

<acceptance_criteria>
- `backend/src/utils/email.ts` existe e exporta `sendResetCodeEmail`
- `backend/package.json` contém `nodemailer` em dependencies
- `backend/.env` contém as variáveis `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`
</acceptance_criteria>
</task>

<task id="11-02-T2">
<title>Adicionar modelo PasswordResetCode ao Prisma e migrar</title>

<read_first>
- backend/prisma/schema.prisma (schema atual)
</read_first>

<action>
1. Adicionar ao `schema.prisma` antes do último `}`:
```prisma
/// Código de reset de senha — expira em 15 minutos
model PasswordResetCode {
  id        String   @id @default(cuid())
  code      String
  expiresAt DateTime
  used      Boolean  @default(false)
  createdAt DateTime @default(now())

  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId String

  @@map("password_reset_codes")
}
```

2. Adicionar relação no modelo `User`:
```prisma
  passwordResetCodes PasswordResetCode[]
```

3. Rodar migration: `npx prisma migrate dev --name add-password-reset-codes`
4. Rodar generate: `npx prisma generate`
</action>

<acceptance_criteria>
- `backend/prisma/schema.prisma` contém `model PasswordResetCode`
- Modelo `User` contém `passwordResetCodes PasswordResetCode[]`
- Migration existe em `backend/prisma/migrations/` com nome contendo `add_password_reset_codes`
- `npx prisma generate` roda sem erros
</acceptance_criteria>
</task>

<task id="11-02-T3">
<title>Criar endpoints de reset de senha no backend</title>

<read_first>
- backend/src/controllers/auth.controller.ts (controller existente)
- backend/src/routes/auth.routes.ts (rotas existentes)
- backend/src/utils/crypto.ts (hashPassword, comparePassword)
- backend/src/utils/email.ts (sendResetCodeEmail criado em T1)
- backend/src/schemas/auth.schema.ts (schemas existentes)
</read_first>

<action>
1. Em `backend/src/schemas/auth.schema.ts`, adicionar:
```ts
export const changePasswordSchema = z.object({
  currentPassword: z.string().min(1, 'Senha atual é obrigatória'),
  newPassword: z.string().min(8, 'Nova senha deve ter no mínimo 8 caracteres'),
});

export const forgotPasswordSchema = z.object({
  email: z.string().email('E-mail inválido'),
});

export const verifyResetCodeSchema = z.object({
  email: z.string().email('E-mail inválido'),
  code: z.string().length(6, 'Código deve ter 6 dígitos'),
});

export const resetPasswordSchema = z.object({
  email: z.string().email('E-mail inválido'),
  code: z.string().length(6, 'Código deve ter 6 dígitos'),
  newPassword: z.string().min(8, 'Nova senha deve ter no mínimo 8 caracteres'),
});
```

2. Em `auth.controller.ts`, adicionar 3 funções:

**changePassword** (requer auth):
- Parsear body com `changePasswordSchema`
- Buscar user por `req.user.id`, incluindo `password`
- `comparePassword(data.currentPassword, user.password)` — se falso: `AppError('Senha atual incorreta', 400)`
- `hashPassword(data.newPassword)` → `prisma.user.update({ where: { id: req.user.id }, data: { password: hashedNew } })`
- `sendSuccess(res, null, 'Senha alterada com sucesso')`

**forgotPassword** (público):
- Parsear body com `forgotPasswordSchema`
- Buscar user por email — se não encontrado, retornar sucesso mesmo assim (segurança: não revelar se email existe)
- Gerar código de 6 dígitos: `Math.floor(100000 + Math.random() * 900000).toString()`
- Invalidar códigos anteriores: `prisma.passwordResetCode.updateMany({ where: { userId: user.id, used: false }, data: { used: true } })`
- Criar registro: `prisma.passwordResetCode.create({ data: { code, userId: user.id, expiresAt: new Date(Date.now() + 15 * 60 * 1000) } })`
- Chamar `sendResetCodeEmail(user.email, code)`
- `sendSuccess(res, null, 'Se o email existir, um código foi enviado')`

**resetPassword** (público):
- Parsear body com `resetPasswordSchema`
- Buscar user por email
- Buscar `PasswordResetCode` com: `{ code, userId: user.id, used: false, expiresAt: { gte: new Date() } }`
- Se não encontrado: `AppError('Código inválido ou expirado', 400)`
- Marcar como usado: `prisma.passwordResetCode.update({ where: { id: resetCode.id }, data: { used: true } })`
- Hash nova senha: `hashPassword(data.newPassword)`
- `prisma.user.update({ where: { id: user.id }, data: { password: hashedNew } })`
- `sendSuccess(res, null, 'Senha redefinida com sucesso')`

3. Em `auth.routes.ts`, adicionar:
```ts
router.post('/change-password', requireAuth, changePassword);
router.post('/forgot-password', forgotPassword);
router.post('/reset-password', resetPassword);
```
</action>

<acceptance_criteria>
- `backend/src/controllers/auth.controller.ts` contém `export async function changePassword`
- `backend/src/controllers/auth.controller.ts` contém `export async function forgotPassword`
- `backend/src/controllers/auth.controller.ts` contém `export async function resetPassword`
- `backend/src/routes/auth.routes.ts` contém `router.post('/change-password'`
- `backend/src/routes/auth.routes.ts` contém `router.post('/forgot-password'`
- `backend/src/routes/auth.routes.ts` contém `router.post('/reset-password'`
- `backend/src/schemas/auth.schema.ts` contém `changePasswordSchema`
- POST `/api/auth/change-password` com senha válida retorna `{ success: true }`
- POST `/api/auth/forgot-password` com email válido retorna `{ success: true }`
</acceptance_criteria>
</task>

<task id="11-02-T4">
<title>Conectar frontend do perfil ao endpoint change-password</title>

<read_first>
- frontend/app/(auth)/settings/profile/ProfileSummary.tsx (componente atual — tem campos de senha mas faz TODO)
- frontend/lib/api/client.ts (API client)
</read_first>

<action>
1. Em `ProfileSummary.tsx`:
   - Substituir o `handleSave` para:
     - Se `newPassword` preenchido: chamar `api.post('/auth/change-password', { currentPassword, newPassword })`
     - Se apenas nome/email alterados: chamar `api.put('/users/${user.id}', { name, email, phone })`  
       (ou um futuro endpoint de profile update — pode usar o updateUser existente se o user tiver permissão)
     - Em caso de sucesso, mostrar toast de sucesso e limpar campos de senha
     - Em caso de erro (ex: senha atual incorreta), mostrar toast de erro com a mensagem do backend
   - Remover o comentário `// TODO: POST /api/auth/profile`
   - Adicionar state `saving` para desabilitar botão durante a requisição
</action>

<acceptance_criteria>
- `ProfileSummary.tsx` NÃO contém `// TODO: POST /api/auth/profile`
- `ProfileSummary.tsx` contém `api.post('/auth/change-password'`
- Botão "Salvar alterações" fica desabilitado durante requisição
- Toast de sucesso aparece após alterar a senha
- Toast de erro aparece se senha atual estiver incorreta
</acceptance_criteria>
</task>

<task id="11-02-T5">
<title>Implementar fluxo de recuperação de senha no frontend</title>

<read_first>
- frontend/app/(public)/recovery/page.tsx (página de recuperação existente)
- frontend/lib/api/client.ts (API client)
</read_first>

<action>
1. Em `frontend/app/(public)/recovery/page.tsx`:
   - Implementar fluxo de 3 steps com estado `step: 'email' | 'code' | 'newPassword'`:
   
   **Step 1 — Email:**
   - Campo de email
   - Botão "Enviar código"
   - Ao clicar: `api.post('/auth/forgot-password', { email })`
   - Sucesso → mover para step 'code'
   
   **Step 2 — Código:**
   - Mostrar mensagem "Um código de 6 dígitos foi enviado para {email}"
   - 6 inputs de 1 dígito cada (ou um input único com maxLength 6)
   - Ao completar 6 dígitos: auto-avançar para step 'newPassword'
   - Botão "Reenviar código" para chamar forgot-password novamente
   
   **Step 3 — Nova Senha:**
   - Campo "Nova senha" (min 8 chars)
   - Campo "Confirmar senha"
   - Botão "Redefinir senha"
   - Ao clicar: `api.post('/auth/reset-password', { email, code, newPassword })`
   - Sucesso → redirecionar para `/login` com toast de sucesso
   - Erro (código expirado) → voltar para step 'email' com mensagem

2. Estilizar usando as mesmas CSS vars e padrões visuais do login/register existentes (rounded-2xl, cores var(--color-*), etc.)
</action>

<acceptance_criteria>
- Página de recovery contém 3 steps visíveis: email, código, nova senha
- `recovery/page.tsx` contém `api.post('/auth/forgot-password'`
- `recovery/page.tsx` contém `api.post('/auth/reset-password'`
- Após redefinir senha com sucesso, redireciona para `/login`
- Input de código aceita exatamente 6 dígitos
</acceptance_criteria>
</task>

## Verificação

- [ ] Alterar senha no perfil funciona (senha atual → nova senha)
- [ ] Senha atual incorreta mostra erro
- [ ] Solicitar reset via email envia código (verificar logs do nodemailer se SMTP não configurado)
- [ ] Código de 6 dígitos expira após 15 minutos
- [ ] Redefinir senha com código válido funciona e redireciona para login
- [ ] Login com nova senha funciona
