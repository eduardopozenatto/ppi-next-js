# Plan 11-03: Exclusão Real (Hard Delete) de Usuários pelo Admin

---
wave: 2
depends_on: [11-01]
files_modified:
  - backend/src/controllers/users.controller.ts
  - backend/src/routes/users.routes.ts
  - frontend/app/(auth)/admin/users/page.tsx
autonomous: true
requirements: [FADM-01]
---

## Objetivo

Quando um admin clica em "Excluir Usuário", o registro deve ser permanentemente removido do banco de dados (hard delete) ao invés de apenas setar `isActive: false` (soft delete). O modal de confirmação deve deixar claro que a ação é irreversível.

## must_haves

- Endpoint `DELETE /api/users/:id` faz `prisma.user.delete()` ao invés de `prisma.user.update({ isActive: false })`
- Registros relacionados (notificações, overrides) são deletados em cascata (já configurado no Prisma com `onDelete: Cascade`)
- Empréstimos do usuário NÃO são deletados em cascata — se o user tem empréstimos ativos/pendentes, o delete é bloqueado
- Modal de confirmação no frontend mostra aviso de irreversibilidade
- Toast de sucesso após exclusão real

## Tarefas

<task id="11-03-T1">
<title>Alterar deleteUser para hard delete no backend</title>

<read_first>
- backend/src/controllers/users.controller.ts (função deleteUser atual — faz soft delete)
- backend/prisma/schema.prisma (relações do User: loans, notifications, permissionOverrides)
</read_first>

<action>
1. Em `backend/src/controllers/users.controller.ts`, substituir a função `deleteUser`:

```ts
export const deleteUser = async (req: Request, res: Response) => {
  try {
    const id = getParam(req.params.id);
    if (!id) return res.status(400).json({ success: false, message: 'ID não fornecido' });

    // Impedir exclusão do próprio usuário
    if (req.user?.id === id) {
      return res.status(400).json({ success: false, message: 'Você não pode excluir sua própria conta' });
    }

    // Verificar se o usuário tem empréstimos ativos ou pendentes
    const activeLoans = await prisma.loan.count({
      where: {
        borrowerId: id,
        status: { in: ['pending', 'active', 'overdue'] },
      },
    });

    if (activeLoans > 0) {
      return res.status(400).json({
        success: false,
        message: `Não é possível excluir: o usuário possui ${activeLoans} empréstimo(s) ativo(s) ou pendente(s). Resolva os empréstimos antes de excluir.`,
      });
    }

    // Hard delete — cascata em notifications e permissionOverrides via Prisma onDelete: Cascade
    await prisma.user.delete({
      where: { id },
    });

    return res.json({ success: true, message: 'Usuário excluído permanentemente' });
  } catch (error: any) {
    if (error.code === 'P2025') {
      return res.status(404).json({ success: false, message: 'Usuário não encontrado' });
    }
    console.error('[deleteUser]', error);
    return res.status(500).json({ success: false, message: 'Erro interno ao excluir usuário', errors: [String(error)] });
  }
};
```

2. Verificar no `schema.prisma` que o modelo `Loan` NÃO tem `onDelete: Cascade` na relação `borrower` — isso é intencional para que empréstimos históricos (returned/cancelled) sobrevivam à exclusão do user. Se a relação não tem onDelete, o Prisma vai bloquear o delete se existirem loans. Nesse caso, antes do `prisma.user.delete`, deletar manualmente os empréstimos com status `returned` ou `cancelled`:

```ts
// Remover empréstimos finalizados (histórico) antes de deletar o user
await prisma.loan.deleteMany({
  where: {
    borrowerId: id,
    status: { in: ['returned', 'cancelled'] },
  },
});
```
</action>

<acceptance_criteria>
- `users.controller.ts` contém `prisma.user.delete(` em vez de `prisma.user.update(` na função deleteUser
- `users.controller.ts` contém check `activeLoans > 0` que bloqueia exclusão se há empréstimos ativos
- `users.controller.ts` contém check `req.user?.id === id` que impede auto-exclusão
- DELETE `/api/users/:id` com user sem empréstimos ativos retorna `{ success: true, message: 'Usuário excluído permanentemente' }`
- DELETE `/api/users/:id` com user com empréstimos ativos retorna status 400
</acceptance_criteria>
</task>

<task id="11-03-T2">
<title>Atualizar modal de exclusão no frontend</title>

<read_first>
- frontend/app/(auth)/admin/users/page.tsx (modal de exclusão atual)
</read_first>

<action>
1. Em `frontend/app/(auth)/admin/users/page.tsx`, atualizar o modal de exclusão:
   - Mudar o texto da confirmação para ser mais claro sobre irreversibilidade:
     ```
     "Tem certeza que deseja excluir permanentemente o usuário "{nome}"? 
     Todos os dados associados (notificações, permissões) serão removidos. 
     Esta ação NÃO pode ser desfeita."
     ```
   - Adicionar ícone de aviso (⚠️) antes do texto
   - Se o backend retornar erro (ex: empréstimos ativos), mostrar a mensagem de erro do backend via toast
   - Mudar a mensagem do toast de sucesso de "Usuário removido" para "Usuário excluído permanentemente"

2. O `handleDelete` já chama `api.del(/users/${deleteModal.id})` — o backend é que muda o comportamento. O frontend só precisa atualizar textos.
</action>

<acceptance_criteria>
- Modal de exclusão contém texto "permanentemente" e "NÃO pode ser desfeita"
- Toast de sucesso mostra "Usuário excluído permanentemente"
- Toast de erro exibe mensagem do backend quando exclusão é bloqueada
- Modal contém ícone de aviso ⚠️
</acceptance_criteria>
</task>

## Verificação

- [ ] Clicar em excluir um user sem empréstimos ativos remove o registro do banco (verificar com `prisma studio`)
- [ ] Clicar em excluir um user COM empréstimos ativos mostra erro
- [ ] Admin não consegue excluir a si próprio
- [ ] Notificações e overrides do user deletado são removidos em cascata
- [ ] User deletado não aparece mais na lista de usuários
