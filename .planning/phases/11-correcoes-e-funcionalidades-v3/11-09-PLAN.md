---
wave: 1
depends_on: []
files_modified:
  - "backend/src/controllers/reports.controller.ts"
autonomous: true
---

# Gap 4: Exportação de Relatórios Incompleta

## Objective
Garantir que a exportação XLSX traga absolutamente todos os registros (sem paginação) e inclua todos os relacionamentos necessários (como Usuários nos empréstimos).

## Tasks

<task>
  <read_first>
    - backend/src/controllers/reports.controller.ts
  </read_first>
  <action>
    No `backend/src/controllers/reports.controller.ts`:
    1. Em `exportInventory`, certifique-se de que a consulta `prisma.inventoryItem.findMany()` não possui as propriedades `take` ou `skip`. Deve retornar todos os itens.
    2. Em `exportLoans`, certifique-se de que a consulta `prisma.loan.findMany()` não possui `take` ou `skip`, e que inclua `{ user: true, items: { include: { inventoryItem: true } } }`. Certifique-se de mapear o `Nome do Usuário` a partir de `loan.user.name` nas colunas do Excel.
  </action>
  <acceptance_criteria>
    - `backend/src/controllers/reports.controller.ts` does not use `take` in `exportInventory`
    - `backend/src/controllers/reports.controller.ts` does not use `take` in `exportLoans`
    - `backend/src/controllers/reports.controller.ts` maps `user.name` or `loan.user.name` in exportLoans
  </acceptance_criteria>
</task>
