---
wave: 1
depends_on: []
files_modified:
  - "backend/src/controllers/auth.controller.ts"
autonomous: true
---

# Gap 3: Exclusão de Usuários com Empréstimos Ativos

## Objective
Permitir que administradores excluam usuários mesmo se eles tiverem empréstimos ativos, deletando os empréstimos em cascata.

## Tasks

<task>
  <read_first>
    - backend/src/controllers/auth.controller.ts
  </read_first>
  <action>
    No `backend/src/controllers/auth.controller.ts`, na função `deleteUser`:
    1. Remova a validação que bloqueia a exclusão se houver empréstimos ativos ou pendentes (bloco `if (activeLoans > 0)`).
    2. Na transação do Prisma, a deleção em cascata (`prisma.loanItem.deleteMany` e `prisma.loan.deleteMany`) já está configurada para buscar pelo `userId`, então ela já deletará todos os empréstimos e itens associados ao usuário que está sendo excluído. Apenas certifique-se de que não haja bloqueios no código impedindo a execução dessa transação.
  </action>
  <acceptance_criteria>
    - `backend/src/controllers/auth.controller.ts` does not contain `if (activeLoans > 0)` inside `deleteUser`
  </acceptance_criteria>
</task>
