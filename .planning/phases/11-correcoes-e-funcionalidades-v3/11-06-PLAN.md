---
wave: 1
depends_on: []
files_modified:
  - "frontend/app/(auth)/admin/users/page.tsx"
autonomous: true
---

# Gap 1: Avatar não aparece na tabela de usuários

## Objective
Implementar a renderização da foto de perfil (`avatar`) do usuário na tabela de listagem de usuários (`/admin/users/page.tsx`), utilizando a função `getStaticUrl` e o componente `next/image`, mantendo a inicial como fallback.

## Tasks

<task>
  <read_first>
    - frontend/app/(auth)/admin/users/page.tsx
    - frontend/lib/static-url.ts
  </read_first>
  <action>
    Modifique o arquivo `frontend/app/(auth)/admin/users/page.tsx` na renderização da coluna "Usuário" (linha ~167).
    Importe `Image` do `next/image` e `getStaticUrl` de `@/lib/static-url`.
    Substitua a div que renderiza apenas `{u.name.charAt(0).toUpperCase()}` por uma lógica que verifica se `u.avatar` existe.
    Se `u.avatar` existir, renderize um `<Image src={getStaticUrl(u.avatar)} alt={u.name} width={36} height={36} className="h-9 w-9 rounded-full object-cover" />`.
    Caso contrário, renderize a div com a inicial como já está feito.
  </action>
  <acceptance_criteria>
    - `frontend/app/(auth)/admin/users/page.tsx` contains `getStaticUrl`
    - `frontend/app/(auth)/admin/users/page.tsx` contains `<Image` component
  </acceptance_criteria>
</task>
